#include <stdio.h>
void main()
{
    int a,b,c,sum=0;
    printf("enter valve for a,b:");
    scanf("%d%d",&a,&b);
    
    while(a<b)
{ for()
    c=a%10;
    sum=sum+(c*c*c);
    a/=10;
}
if(b==sum)
printf("%d",b);
}
